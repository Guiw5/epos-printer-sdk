# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.qodo
.cursorignore

# Environment variables
.env
.env.test

# Official Epson reference manuals (large PDFs, re-downloadable from
# files.support.epson.com / download3.ebz.epson.net — see README)
sdk/manuals/*.pdf

# React example app (its own dependency tree)
examples/react-app/node_modules
examples/react-app/dist
# Installed agent skills — vendored copies, restored with `npx skills add`.
# skills-lock.json is tracked so the set stays reproducible.
.agents/skills/
.claude/skills/
